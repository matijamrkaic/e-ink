# Python
__pycache__/
*.pyc

# Virtual environments
venv/
.venv/
path/to/venv/

# Local secrets — never commit. Copy .env.example → .env and fill it in.
.env

# Garmin token store / exported token blobs — never commit these
.garminconnect/
garmin_tokens*
*.b64

# Generated dashboard output (regenerated by CI / each run)
dashboard.png
dashboard_debug.png
